{
  "name": "circle-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check js/core.js && node --check js/storage.js && node --check js/app.js && node --check service-worker.js && node --check tools/serve.mjs",
    "test": "node --test",
    "serve": "node tools/serve.mjs"
  }
}
