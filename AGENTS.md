# Codex作業ルール

このリポジトリでCodexが作業する際は、以下のルールに従う。

## プロジェクト概要

- `circle-planner`は、iPadとPCで使用する24時間円形スケジュール管理用のPWAである。
- 1日の予定を、カテゴリーごとに色分けした扇形として24時間の円上へ表示する。
- UIの主役は円形スケジュールとし、時刻、予定名、選択状態を高い視認性で示す。
- UIを新規作成・変更する場合は、リポジトリ直下の`UI_STYLE_GUIDE.md`を確認し、プロジェクト固有の指針を優先する。

## プロジェクト固有の実装方針

- ダークモードを基本とし、円形スケジュールの時刻と予定名を十分なコントラストで読みやすくする。
- 予定はカテゴリーごとの色付き扇形で表現する。ただし、色だけに依存せず、予定名、境界線、選択表示などでも判別できるようにする。
- iPadのタッチ操作を重視し、主要操作のタップ領域は原則として44px以上を確保する。
- タッチ専用の操作だけに依存せず、PCのマウス操作や利用可能なキーボード操作も維持する。
- iPadとPCの両方で、円形表示、操作パネル、ダイアログが崩れないレスポンシブUIにする。
- PWAのマニフェスト、Service Worker、キャッシュ対象やバージョンを変更する場合は、インストール、更新、オフライン利用への影響を確認する。
- 予定や設定の保存方式を変更する場合は、既存の保存データとの互換性を維持し、移行が必要なら安全な移行処理と確認方法を用意する。
- 参考アプリの機能や操作の考え方は参考にしてよいが、アイコン、レイアウト、装飾などをそのまま複製しない。

## 作業ブランチ

- このリポジトリでCodexが使用するブランチは `codex-work` とする。
- 作業開始前に現在のブランチを確認する。
- `codex-work` 以外の場合は、ファイルを変更せずユーザーへ報告する。
- `main`には直接コミット・プッシュしない。

## 作業前の確認

- 作業開始前に `git status` を確認する。
- ユーザーが作成した未コミットの変更を削除・上書き・破棄しない。
- 関係のないファイルを変更しない。

## 実装前の説明

- 実装前に、作業の重さ、変更対象、既存機能への影響、考えられるリスクを簡潔に説明する。
- 作業を安全な単位に分けた方がよい場合は、その理由と分割案を示す。
- 説明後、ユーザーの追加判断が必要な重大な選択肢がなければ、そのまま実装を続ける。

## 任意のUIデザイン規約

- リポジトリ直下に `UI_STYLE_GUIDE.md` が存在する場合、UIの新規作成・変更前に内容を読み、ユーザーから別のデザイン指定がない範囲で従う。
- `UI_STYLE_GUIDE.md` が存在しない場合、共通のUIデザインは強制しない。
- 既存プロジェクトに確立されたデザインや、ユーザーの明示的な指定がある場合は、そちらを優先する。
- RPGツクールMZのプラグインなど、ホスト側のUI規約に従う成果物では、明示的な依頼がない限り独自のUI規約を持ち込まない。

## 実装と検証

- 依頼された範囲だけを変更する。
- 既存の操作感と保存データとの互換性を可能な限り維持する。
- 利用可能なテストや構文チェックがあれば、変更後に実行する。
- テストに失敗した場合は自動プッシュせず、原因をユーザーへ報告する。
- 秘密情報、`.env`、ログ、キャッシュ、一時ファイル、ビルド生成物、ユーザーが作成したデータをコミットしない。
- UIを変更した場合は、可能な範囲でiPad相当のタッチ画面とPC画面の両方を確認し、未確認の実機条件があれば明記する。
- 円形スケジュールを変更した場合は、日付境界、短時間の予定、連続・重複する予定、予定名の長さによる表示崩れを確認する。
- PWA関連を変更した場合は、利用可能な範囲でインストール要件、更新動作、オフライン起動、キャッシュ更新を確認する。

## 自動コミットとプッシュ

- 依頼された変更が完了し、確認やテストに問題がなければ、`git status`と`git diff`で変更内容を確認する。
- 今回の作業で変更したファイルだけを個別に`git add`する。
- 関係のない既存変更はコミットに含めない。
- 内容が分かる簡潔なコミットメッセージを付けてコミットする。
- コミット後、`origin`の`codex-work`ブランチへ自動的にプッシュする。
- 変更がない場合は、空のコミットやプッシュを行わない。

## 禁止事項

- `main`への直接プッシュ
- force push
- ユーザーの許可がないブランチの削除
- ユーザーの許可がないマージやPull Request作成
- 競合の強制的な解決
- `git reset --hard`などによる変更の破棄
- ユーザーの依頼がないZIPや変更ファイルの複製

## 問題発生時

- Git競合、認証エラー、通信エラー、テスト失敗が発生した場合は、無理に処理を続けずユーザーへ報告する。
- 対象コミットや変更範囲が不明確な場合は、推測で取り消し・復元を実行しない。
- 作業完了時に、変更したファイル、実行した確認、コミットID、プッシュ先ブランチを報告する。
