# ダークフレームUI デザインガイド

このファイルは、ダークフレームUIを基準に、`circle-planner`の新しいUIや、デザイン指定のないUI部品を作る際の指針として使用する。

## 適用範囲

- 新しい単独Webツール、デスクトップツール、管理画面などに適用する。
- ユーザーがUIを指定していない場合や、ボタン・入力欄・メニューなどのデザインに迷う場合の標準とする。
- ユーザーが別のデザインを指定した場合は、そちらを優先する。
- 既存プロジェクトに確立されたデザインがある場合は、勝手に全面変更しない。
- RPGツクールMZのプラグイン、Godot・Unity内のゲームUI、外部アプリへ組み込む拡張機能など、ホスト側のデザイン規約に従う必要がある場合は、明示的な依頼がない限り適用しない。
- 依頼された画面・部品以外まで一括でデザイン変更しない。

## デザインの方向性

- ダークモードを基本とし、明るい場所と暗い場所のどちらでも主要情報を読み取りやすくする。
- 黒に近い背景と、少し明るい濃紺・チャコールのパネルを組み合わせる。
- 重要なパネルや入力欄は、白または明るいグレーの枠線で明確に囲む。
- 枠線と角丸を主役にし、派手なグラデーション、強い光彩、過剰な影は使わない。
- 文字は白に近い明るいグレーを基本とし、補足情報は少し暗いグレーにする。
- 選択中・有効状態には落ち着いた緑を使用する。
- 高コントラストで落ち着きがあり、長時間見ても疲れにくいUIを目指す。
- PCだけでなく、iPadなどのタッチ操作でも使いやすくする。

## circle-planner固有の方向性

- 24時間の円形スケジュールを画面の主役にし、補助パネルや装飾が円より強く目立たないようにする。
- 予定は開始時刻から終了時刻までの範囲を、カテゴリーごとの色付き扇形として表示する。
- 円周上の時刻、予定名、現在時刻、選択状態は、背景や扇形に埋もれない高コントラストな表示にする。
- カテゴリーは色だけで識別させず、予定名、明瞭な境界線、選択時の輪郭や詳細表示も併用する。
- 短時間の予定や隣接する予定でも、境界と選択対象が分かるようにする。
- iPadでは指で円や予定を操作することを前提とし、操作対象を密集させすぎない。
- PCでは円形表示と操作領域のバランスを保ち、広い画面を使っても円や文字が過度に拡大・分散しないようにする。
- 参考アプリから機能や操作上の考え方を学ぶことはできるが、アイコン、レイアウト、装飾、配色の組み合わせなどをそのまま複製しない。

## 基本カラートークン

実装環境に合わせて微調整してよいが、色調の関係は維持する。

| 用途 | 推奨色 |
| --- | --- |
| ページ背景 | `#0d1117` |
| ヘッダー背景 | `#0f141b` |
| メインパネル | `#171d25` |
| 補助パネル・ボタン | `#1b232d` |
| 通常枠線 | `#34404d` |
| 強調枠線 | `#d5d9e2` |
| メイン文字 | `#f1f3f5` |
| 補助文字 | `#aeb7c2` |
| アクセント緑 | `#3f8f55` |
| 危険操作 | `#b84a4a` |

カテゴリー色は、暗い背景上で互いを識別でき、扇形の境界、時刻、予定名の可読性を損なわない色を選ぶ。カテゴリー数が増えても、似た色を隣接させるだけで区別しようとせず、ラベルや境界線を併用する。

## CSS変数の基準例

Web UIでは、色や角丸を直接何度も記述せず、共通変数として管理する。

```css
:root {
  --ui-bg: #0d1117;
  --ui-header: #0f141b;
  --ui-panel: #171d25;
  --ui-control: #1b232d;
  --ui-border: #34404d;
  --ui-border-strong: #d5d9e2;
  --ui-text: #f1f3f5;
  --ui-text-muted: #aeb7c2;
  --ui-accent: #3f8f55;
  --ui-danger: #b84a4a;
  --schedule-ring: #526170;
  --schedule-hour-mark: #d5d9e2;
  --schedule-now: #f0c75e;
  --ui-radius-sm: 8px;
  --ui-radius-md: 12px;
  --ui-space-sm: 8px;
  --ui-space-md: 12px;
  --ui-space-lg: 20px;
}
```

既存プロジェクトにすでにデザイントークンがある場合は、新しい変数を重複して増やさず、既存の仕組みへ統合する。

## パネルとウィンドウ

- 背景は`--ui-panel`を基本とする。
- 通常の補助パネルは`1px`の通常枠線を使う。
- 重要な表示領域やフォーカス対象は`2px`程度の明るい強調枠線を使う。
- 角丸は`8px`から`12px`程度とする。
- 内側余白は`12px`から`20px`を基本とし、文字やボタンを枠へ密着させない。
- 白い強調枠線をすべての要素へ多用せず、主役となる領域に絞る。

## 円形スケジュール

- 24時間の円を最優先の表示領域とし、画面内で可能な限り大きく見せながら、時刻や予定名が切れない余白を確保する。
- 0時を含む基準位置と時間の進行方向を一貫させ、主要な時刻目盛りは補助目盛りより強く表示する。
- 扇形同士の境界には暗背景でも見える線または間隔を設け、隣接する同系色の予定も判別できるようにする。
- 予定名は扇形内の可読性を優先し、収まらない場合は省略表示と選択時の詳細表示などを組み合わせる。
- 文字色は扇形の色に応じて明色・暗色を使い分け、時刻と予定名は通常サイズで少なくとも`4.5:1`程度のコントラスト比を目安にする。
- 現在時刻、選択中の予定、編集対象は、カテゴリー色とは別の輪郭、マーカー、太さなどで区別する。
- 円上の細い扇形を直接選びにくい場合は、近傍選択、一覧、詳細パネルなど、同じ予定へ到達できる補助手段を用意する。

## ボタン

- 濃い背景、控えめな枠線、明るい文字を基本とする。
- ボタン同士で高さ、角丸、余白、文字サイズを統一する。
- 選択中・有効状態はアクセント緑で分かりやすく示す。
- 危険な操作だけ危険色を使用する。
- iPadなどを考慮し、主要操作のタップ可能領域は幅・高さともに原則`44px`以上にする。見た目が小さいアイコンでも、透明な余白を含む操作領域を確保する。
- ホバーだけで状態を伝えず、選択・押下・無効状態をタッチ操作でも識別できるようにする。
- ドラッグや長押しだけを唯一の操作方法にせず、必要に応じてタップで選べる代替操作を用意する。

## 入力欄・メニュー・ダイアログ

- 入力欄は暗い背景と明瞭な枠線を使い、フォーカス時は枠線またはアクセント色を強める。
- プレースホルダーと入力済み文字を十分に区別する。
- メニューは背景と同化させず、枠線または小さな影で前面にあることを示す。
- ダイアログは背後を暗くし、主要操作とキャンセル操作を明確に分ける。
- 確認なしでは戻しにくい操作は、誤操作を防ぐ配置・文言にする。

## レイアウトとレスポンシブ対応

- 主要情報には十分な余白を取り、狭い画面では横並びを無理に維持しない。
- PC・iPadの両方で、文字切れ、横スクロール、ボタンの押しづらさがないか確認する。
- 固定幅だけに依存せず、必要に応じて`min()`、`max()`、`clamp()`、Flexbox、Gridなどを使用する。
- セーフエリアや画面端に操作ボタンが密着しないようにする。
- iPadでは縦向き・横向きの両方を想定し、円形スケジュールと編集領域の優先順位を保つ。
- 狭い画面では補助パネルを円の下やダイアログへ移し、円の可読性を犠牲にして横並びを維持しない。
- PCの広い画面では円の最大サイズと周辺パネルの読みやすい幅を制御し、過度な余白や視線移動を避ける。
- `env(safe-area-inset-*)`を必要に応じて利用し、PWAとして全画面表示した際も主要操作を安全領域内に置く。

## 実装ルール

- 色、枠線、角丸、余白、文字サイズは可能な限り共通トークンで管理する。
- 同じ見た目のボタンやパネルを画面ごとに重複実装しない。
- 既存コンポーネントがある場合は再利用する。
- 見た目の変更によって既存の操作、保存データ、キーボード操作、タッチ操作を壊さない。
- 装飾より、読みやすさ、押しやすさ、状態の分かりやすさを優先する。

## 完了前チェック

- [ ] 背景・パネル・枠線・文字色が共通トークンで統一されている
- [ ] 白い強調枠線を必要な場所だけに使用している
- [ ] ボタンの高さ、角丸、余白が統一されている
- [ ] 選択中・無効・危険操作の状態が区別できる
- [ ] iPadで押せる十分なタップ領域がある
- [ ] 24時間の円、時刻、予定名、現在時刻、選択状態が明瞭に読める
- [ ] カテゴリーの扇形を色だけに依存せず判別できる
- [ ] 短時間、隣接、日付境界の予定を選択・確認できる
- [ ] iPadの縦向き・横向きとPC表示で、円と操作領域が崩れない
- [ ] 狭い画面でも文字切れや不自然な横スクロールがない
- [ ] 既存機能や保存データに影響していない
- [ ] ユーザーの明示的なデザイン指定を上書きしていない
